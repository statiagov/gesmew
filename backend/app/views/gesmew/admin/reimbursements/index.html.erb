<%= render partial: 'gesmew/admin/shared/order_tabs', locals: { current: :reimbursements } %>

<% content_for :page_title do %>
  / <%= Gesmew.t(:edit_reimbursement) %>
<% end %>

<%= render partial: 'gesmew/admin/shared/error_messages', locals: { target: @customer_return } %>

<table class="table">
  <thead data-hook="customer_return_header">
    <tr>
      <th><%= Gesmew.t(:id) %></th>
      <th><%= Gesmew.t(:total) %></th>
      <th><%= Gesmew.t(:status) %></th>
      <th><%= "#{Gesmew.t('date')}/#{Gesmew.t('time')}" %></th>
    </tr>
  </thead>
  <tbody>
    <% @reimbursements.each do |reimbursement| %>
      <tr id="<%= gesmew_dom_id(reimbursement) %>" data-hook="reimbursement_row">
        <td><%= reimbursement.id %></td>
        <td><%= reimbursement.total %></td>
        <td><%= reimbursement.reimbursement_status %></td>
        <td><%= pretty_time(reimbursement.created_at) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
