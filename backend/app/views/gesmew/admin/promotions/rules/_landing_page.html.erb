<div class="panel-body">
  <div class="form-group">
    <label for="<%= "#{param_prefix}_preferred_path" %>"><%= Gesmew.t('promotion_rule_types.landing_page.description') %>:</label>
    <%= text_field_tag "#{param_prefix}[preferred_path]", promotion_rule.preferred_path, :class => 'form-control' %>
    <p class="help-block">
      <%= Gesmew.t('landing_page_rule.must_have_visited_path') %>
    </p>
  </div>
</div>
