<% content_for :page_title do %>
  <%= Gesmew.t(:new_promotion) %>
<% end %>

<%= form_for :promotion, :url => collection_url do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <div class="text-center">
    <%= render :partial => 'gesmew/admin/shared/new_resource_links' %>
  </div>
<% end %>
