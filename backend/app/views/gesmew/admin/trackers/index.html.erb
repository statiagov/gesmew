<% content_for :page_title do %>
  <%= Gesmew.t(:analytics_trackers) %>
<% end %>

<% content_for :page_actions do %>
  <%= button_link_to Gesmew.t(:new_tracker), new_object_url, :class => "btn-success", :icon => 'add', :id => 'admin_new_tracker_link' %>
<% end if can? :create, Gesmew::Tracker %>

<% if @trackers.any? %>
  <table class="table">
    <colgroup>
      <col style="width: 30%">
      <col style="width: 40%">
      <col style="width: 15%">
      <col style="width: 15%">
    </colgroup>
    <thead>
      <tr data-hook="admin_trackers_index_headers">
        <th><%= Gesmew.t(:google_analytics_id) %></th>
        <th class="text-center"><%= Gesmew.t(:active) %></th>
        <th class="actions"></th>
      </tr>
    </thead>
    <tbody>
      <% @trackers.each do |tracker|%>
        <tr id="<%= gesmew_dom_id tracker %>" data-hook="admin_trackers_index_rows">
          <td><%= tracker.analytics_id %></td>
          <td class="text-center"><%= tracker.active ? Gesmew.t(:say_yes) : Gesmew.t(:say_no) %></td>
          <td class="actions text-right">
            <%= link_to_edit(tracker, :no_text => true) if can? :edit, tracker %>
            <%= link_to_delete(tracker, :no_text => true) if can? :delete, tracker %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class="alert alert-warning">
    <%= Gesmew.t(:no_resource_found, resource: plural_resource_name(Gesmew::Tracker)) %>,
    <%= link_to(Gesmew.t(:add_one), new_object_url) if can? :create, Gesmew::Tracker %>!
  </div>
<% end %>
