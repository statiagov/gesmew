<% content_for :page_actions do %>
  <% if can?(:create, Gesmew::Product) %>
    <%= button_link_to Gesmew.t(:new_product), new_object_url, { class: "btn-success", icon: 'add', id: 'admin_new_product' } %>
  <% end %>
<% end %>

<%= render partial: 'gesmew/admin/shared/product_tabs', locals: {current: :details} %>
<%= render partial: 'gesmew/admin/shared/error_messages', locals: { target: @product } %>

<%= form_for [:admin, @product], method: :put, html: { multipart: true } do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'gesmew/admin/shared/edit_resource_links' %>
  </fieldset>
<% end %>
