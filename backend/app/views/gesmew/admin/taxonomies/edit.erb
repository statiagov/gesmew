<%= render partial: 'js_head' %>

<% content_for :page_title do %>
  <%= Gesmew.t(:taxonomy_edit) %>
<% end %>

<div id="ajax_error" class="errorExplanation" style="display:none;"></div>

<%= form_for [:admin, @taxonomy] do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <div class="form-group">
      <%= label_tag nil, Gesmew.t(:tree) %>
      <script>
        Gesmew.routes.taxonomy_taxons_path = "<%= gesmew.api_v1_taxonomy_taxons_path(@taxonomy) %>";
        Gesmew.routes.admin_taxonomy_taxons_path = "<%= gesmew.admin_taxonomy_taxons_path(@taxonomy) %>";
        Gesmew.routes.jstree_theme_path = "<%= stylesheet_path("jquery.jstree/themes/gesmew/style.css") %>";
      </script>
      <div id="taxonomy_tree" class="tree">
        <div class="alert alert-warning">
          <%= Gesmew.t(:loading_tree) %>
        </div>
      </div>
    </div>

    <div class="alert alert-info">
      <%= Gesmew.t(:taxonomy_tree_instruction) %>
    </div>

    <div class="form-actions">
      <%= button Gesmew.t('actions.update'), 'ok', 'submit', { class: 'btn-success' } %>
      <span class="or"><%= Gesmew.t(:or) %></span>
      <%= button_link_to Gesmew.t('actions.cancel'), admin_taxonomies_path, icon: 'delete' %>
    </div>
  </fieldset>
<% end %>
