<% content_for :page_title do %>
  <%= plural_resource_name(Gesmew::ReimbursementType) %>
<% end %>

<% if @reimbursement_types.any? %>
  <table class="table" id='listing_reimbursement_types' data-hook>
    <thead>
      <tr data-hook="reimbursement_types_header">
        <th><%= Gesmew.t(:name) %></th>
        <th><%= Gesmew.t(:type) %></th>
      </tr>
    </thead>
    <tbody>
      <% @reimbursement_types.each do |reimbursement_type| %>
        <tr id="<%= gesmew_dom_id reimbursement_type %>" data-hook="reimbursement_type_row">
          <td class="align-center">
            <%= reimbursement_type.name.humanize %>
          </td>
          <td>
            <%= reimbursement_type.type %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class="alert alert-info no-objects-found">
    <%= Gesmew.t(:no_resource_found, resource: plural_resource_name(Gesmew::ReimbursementType)) %>
  </div>
<% end %>
