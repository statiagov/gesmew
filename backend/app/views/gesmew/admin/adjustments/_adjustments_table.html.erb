<div class="panel-heading">
  <h3 class="panel-title"><%= Gesmew.t(:order_adjustments) %></h3>
</div>

<table class="table table-bordered adjustments" data-hook="adjustments">
  <thead data-hook="adjustmment_head">
    <tr>
      <th><%= Gesmew.t(:adjustable) %></th>
      <th><%= Gesmew.t(:description) %></th>
      <th class="text-center"><%= Gesmew.t(:amount) %></th>
      <th class="text-center"><%= Gesmew.t(:state) %></th>
      <th class="actions"></th>
    </tr>
  </thead>
  <tbody>
    <%= render partial: "adjustment", collection: @adjustments %>
  </tbody>
</table>

<% if can?(:edit, Gesmew::Adjustment) %>
  <div class="panel-footer">
    <div class="row text-center">
      <div class="col-md-6">
        <%= button_to Gesmew.t(:open_all_adjustments), open_adjustments_admin_order_path(@order), method: :get, class: "btn btn-success" %>
      </div>
      <div class="col-md-6">
        <%= button_to Gesmew.t(:close_all_adjustments), close_adjustments_admin_order_path(@order), method: :get, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
<% end %>
