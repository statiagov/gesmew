<%= render partial: 'gesmew/admin/shared/order_tabs', locals: { current: :adjustments } %>

<% content_for :page_title do %>
  / <%= Gesmew.t(:edit) %> <%= Gesmew.t(:adjustment) %>
<% end %>

<%= render partial: 'gesmew/admin/shared/error_messages', locals: { target: @adjustment } %>

<%= form_for @adjustment, url: admin_order_adjustment_path(@order, @adjustment), method: :put do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>

    <div class="form-actions" data-hook="buttons">
      <%= button Gesmew.t(:continue), 'save' %>
      <span class="or"><%= Gesmew.t(:or) %></span>
      <%= button_link_to Gesmew.t('actions.cancel'), gesmew.admin_order_adjustments_url(@order), icon: 'delete' %>
    </div>
  </fieldset>
<% end %>
